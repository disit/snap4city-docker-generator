-1) make a backup
0) have a nifi already set up
1) turn off nifi instances
2) copy (not cut!) the nifi folder from the original host to the additional host (the original is going to be called "first")
3) extract the nifi yaml section from the docker compose file and copy it in a new docker compose file in the additional host
4) patch the yamls with the provided yamls (docker-compose -f base.yaml -f patch.yaml config > out.yaml)
5) start again nifi on both instances
